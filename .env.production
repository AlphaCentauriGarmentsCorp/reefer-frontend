# REEFER storefront — production build config
# Auth/account is hidden for the coming-soon launch, so the app makes NO API calls yet.
# When the backend goes live, point this at its public API base, e.g.:
#   VITE_API_URL=https://api.reeferclothing.com/api/v2
#
# NOTE: axios reads VITE_API_URL (src/api/axios.js) but src/config/index.js reads
#       VITE_API_BASE_URL. Reconcile to ONE name when you wire the backend.
# VITE_API_URL=
